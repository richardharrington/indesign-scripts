/*    This script goes through and clears    away unysed URLDestination objects from the document,    in case they've been piling up because    people have been creating new documents from    old ones instead of from templates.    */#include utilities/util.jsxvar Util = FORWARD.Util;var doc = app.activeDocument;var destinationsToSave = Util.forEach( doc.hyperlinks, function( link ) {    return link.destination;});Util.forEach( doc.hyperlinkURLDestinations, function( dest ) {    if (!Util.isIn( dest, destinationsToSave )) {        dest.remove();    }});alert("Unused URL Destinations cleared");    